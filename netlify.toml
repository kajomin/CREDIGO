# Ensures the PDF attachment gets bundled together with the on-form function
# (Netlify's function bundler only picks up JS/TS by default; non-code assets
# need to be listed explicitly).
[functions."on-form"]
  included_files = ["netlify/functions/assets/*.pdf"]
