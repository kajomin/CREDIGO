# NOTE: this file only works on Netlify. If you deploy this site on
# GitHub Pages instead, it is ignored — GitHub Pages has no redirect engine.
#
# Domain canonicalization (apex vs www) is handled by Netlify's own
# Domain management settings, not here — credigo.cz is configured there
# as the primary domain, with www.credigo.cz redirecting to it automatically.
# Do NOT add apex/www redirect rules in this file: they fight Netlify's
# built-in domain redirect and cause a redirect loop (ERR_TOO_MANY_REDIRECTS).
